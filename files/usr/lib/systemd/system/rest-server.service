[Unit]
Description=restic REST server (rest-server)
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/rest-server --path /var/lib/rest-server --no-auth --listen :80
Restart=on-failure

[Install]
WantedBy=multi-user.target
